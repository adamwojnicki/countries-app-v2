import React from "react";
import { CountryList } from "../../components/CountryList";

const Countries = () => {
  return (
    <div>
      <h2>Countries</h2>
      <CountryList />
    </div>
  );
};

export default Countries;
